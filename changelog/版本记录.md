# 木安-AI分身 版本记录

## v1.5.1 — 2026-05-09

**Skills 索引去死文件 + 生命周期协议放宽**

- 删除 `skills/每周求职复盘.md`：原设计依赖 cron 自动触发，但本地 Claude Code 无后台调度能力，触发机制从一开始就跑不通；木安实际三个月未手动触发。设计错误，无回溯价值，直接删。
- `CLAUDE.md` Skills 索引段补全：新增 `面试准备.md`（5/8 沉淀的素材库 + AI/LLM 认知答法骨架 + 职业规划三段式骨架，之前因未进索引一直没被加载），同步修正 `项目讲述.md` 的触发描述（避免与"面试准备"语义重叠）。
- `CLAUDE.md` Skills 生命周期协议放宽：原"归档不删除"改为三态——活跃保留 / 暂时不用归档 / **设计错误或无回溯价值可直接删**。

**架构意义**：补上"索引-文件"一致性的运维闭环。死 skill（设计错误或未进索引）等于不存在，反而拖累协议可信度。本次清理让 6 个 skill 全部可被正确触发。

---

## v1.5 — 2026-04-16

**扩充 Skills 库 + 新增工作纪律规则**

- 新增 3 个 Skill：`写cold_message.md`（冷联系消息）、`面试复盘.md`（面试后结构化复盘）、`公司调研.md`（目标公司调研报告）。Skills 总数从 2 → 5。
- `CLAUDE.md` 新增"工作纪律"节：约束先行（先改文档再改实践）、改完验证（不只改不验）、git push 等指令。
- `CLAUDE.md` Skills 注册列表同步更新。

**架构意义**：分身从"骨架完整但肌肉不足"升级到求职场景全覆盖——拆 JD → 公司调研 → 写消息 → 面试讲述 → 面试复盘，形成求职闭环。工作纪律补全了执行质量的底线约束。

---

## v1.4 — 2026-04-15

**加入结束协议，补全跨 session 状态追踪**

- `CLAUDE.md` 新增"结束协议"：session 结束前自动将本次对话关键结论压缩为 1-3 条，写入记忆文件。
- `memory/当前任务版.md` 顶部新增"上次对话摘要"字段，作为跨 session 状态衔接的载体。

**架构意义**：分身从"启动读取"单向补全为"启动读取 + 结束写回"双向闭环，跨 session 连续性从依赖手动更新升级为协议保障。

---

## v1.3 — 2026-04-14

**加入 Sensor + Hook，补全 Guide-only 架构短板**

- `CLAUDE.md` 复利工程新增第 5 步：写回前强制执行三问自查（Sensor），防止错误内容进入记忆。
- `CLAUDE.md` 新增 Skills 按需加载规则：只有场景明确匹配时才读取 skill 文件；归档方式为文件重命名（加 `archived_` 前缀），不删除。
- `.claude/settings.json` 新增 PostToolUse hook：每次 Write 工具写入 memory/ 或 CLAUDE.md 时，自动记录时间戳 + 路径到 `changelog/memory_log.txt`（外部 Sensor，可追溯写回历史）。
- `.claude/log_memory_write.py` 新建：hook 对应的 Python 脚本。

**架构意义**：分身从"纯 Guide"升级为"Guide + Sensor"双机制，对应 Harness Engineering 框架中 feedforward + feedback 的完整闭环。

**启动规则（重要）**：`.claude/settings.json` 的权限配置只在以 `木安-AI分身/` 为根目录启动 Claude Code 时生效。必须用以下方式启动，否则 Edit/Write 每次都会弹确认框：
```bash
cd "/Volumes/T7 Shield/AI-workspace/木安-AI分身"
claude
```
或在 VSCode 中以该目录为工作区打开。**不要从其他目录启动后跨目录操作分身文件。**

## v1.2 — 2026-04-06

**从"文档版"升级到"可用版"**

- 精简 `CLAUDE.md`：233 行 → ~95 行，去除重复、压缩描述、保留核心规则。
- 新增启动协议：每次对话开始自动读取记忆文件、确认主线任务。
- 填充 `memory/长期画像.md`：完整写入当前背景、目标、项目、求职状态。
- 填充 `memory/当前任务版.md`：写入求职主线、三项任务、卡点。
- 优化外部协议引用：改为"需要时读取"而非每次全量加载，降低 token 开销。

## v1.1 — 2026-04-04

**从“人设版”升级到“可执行版”**

- 新增 `协议/红黄绿灯执行协议.md`：把红黄绿灯从原则改为可执行检查与话术模板。
- 新增 `协议/记忆写回协议.md`：定义长期/短期记忆边界与写回标准。
- 新增 `评测/分身评测题库.md`：固定 20 题评测，验证版本升级是否真实有效。
- 新增 `评测/版本评测记录.md`：沉淀每次升级效果，支持回滚与对比。
- 新增 `作战手册/任务闭环模板.md`：统一任务输入输出，避免空泛建议。
- 新增 `作战手册/模式切换手册.md`：引入 AI as Me / Team / Alien 三视角。
- 新增 `作战手册/每周进化清单.md`：固定每周进化节奏。
- 升级 `memory/当前任务版.md`：从空模板改为结构化执行模板。
- 升级 `memory/长期画像.md`：补充证据、边界、复核字段。
- 升级 `CLAUDE.md`：接入外部协议文件，形成“主宪法 + 执行协议”组合。

## v1.0 — 2026-04-04

**初始化版本**

- 建立 AI 分身项目结构
- 创建 CLAUDE.md 主宪法（完整身份定义、核心目标、行为规则、权限体系、记忆机制、工作流、输出格式）
- 创建 memory/长期画像.md 模板
- 创建 memory/当前任务版.md 模板
- 创建 changelog/版本记录.md

**文件结构：**
```
木安-AI分身/
├── CLAUDE.md              # 主宪法
├── memory/
│   ├── 长期画像.md         # 持续维护的木安画像
│   └── 当前任务版.md       # 当前任务状态
└── changelog/
    └── 版本记录.md         # 版本迭代记录
```

**下一步：**
- 填写当前任务版
- 逐步填充长期画像
- 根据实际使用反馈迭代宪法
